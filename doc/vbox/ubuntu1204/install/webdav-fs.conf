$INSTALL_BASE = '/etc/webdavcgi/';

$BACKEND = 'RCS';
$BACKEND_CONFIG{RCS}={backend=>'FS', bindir=>'/usr/bin', maxrevisions=>4, rcsdirname=>'.rcs', virtualrcsdir=>'REVISIONS'};

$VIRTUAL_BASE = '/';
$DOCUMENT_ROOT = '/data/';


$DBI_SRC='dbi:Pg:dbname=webdavcgi;host=localhost;port=5432';
$DBI_USER='webdavcgi';
$DBI_PASS='changeme';
$CREATE_DB = 0;


$BACKEND_CONFIG{FS} = { fsvlink=> { '/data/' => {  
                'home' => "/home/$ENV{REMOTE_USER}", 
#               'afs' => '/afs'
                }  
} };

